# Definimos una tupla con algunos elementos
tupla = (1, 2, 3, 4, 5,8,13)

# Usamos un bucle for para cada elemento de la tupla
for elemento in tupla:
    # Verificamos si el elemento es menor que 5
    if elemento < 5:
        # Imprimimos el elemento si cumple la condición
        print(elemento)

